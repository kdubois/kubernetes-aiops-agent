# Test configuration
# Provides a dummy token so @ConfigProperty(name = "github.token") is satisfied in @QuarkusTest classes.
github.token=test-token

# Speed up @Retry in tests — no sleep between attempts
dev.kevindubois.rollout.agent.service.AnalysisService/analyze/Retry/delay=0
dev.kevindubois.rollout.agent.service.AnalysisService/analyze/Retry/jitter=0
